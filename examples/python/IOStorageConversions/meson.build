test(
  'SHConversions',
  python,
  args: [files('SHConversions.py'),
         meson.current_source_dir() / '../../ExampleDataFiles'],
  timeout: 60,
  suite: 'python')

test(
  'SHStorage',
  python,
  args: files('SHStorage.py'),
  suite: 'python')
