LICENSE INFORMATION
===================


INSTALLATION INSTRUCTIONS
=========================

1. check and adjust the Makefile

2. type: make all

3. type: make python-wrapper




MINI EXAMPLES
=============

typical imports
---------------
>> import numpy as np
>> import pyshtools

creating coefficient array
--------------------------
>> lmax = 80
>> coeffs = np.random.normal(size=2*(lmax+1)*(lmax+1)).reshape(2,lmax+1,lmax+1)
or
>> coeffs = np.zeros( (2,lmax+1,lmax+1) )

forward and inverse spherical harmonic transforms
-------------------------------------------------
>> grid = pyshtools.MakeGridDH(coeffs)
>> coeffs = pyshtools.SHExpandDH(grid)




KNOWN ISSUES
============

assumed array shapes
--------------------
assumed array shapes are not supported in C or Python and compiler dependent. The file 
PythonWrapper.f95 provides wrapper subroutines that converts arrays from assumed to
explicit shapes.


optional return parameters
--------------------------
optional return parameters are not supported in C or Python. We either have
to return them always which is memory intensive and not good for speed, or
we need two separate subroutines. Affected routines:


fortran 95's intrinsic 'present()' procedure 
--------------------------------------------
This is also not supported in C or Python. We always pass all optional
arguments. If their value is not given they are filled with a default.


fortran's 'stop' statement
--------------------------
unfortunately this also kills the underlying Python interpreter if used in
interactive mode. Fortran error's can not be caught from Python.




LIST OF WRAPPED AND UNWRAPPED FUNCTIONS 
=======================================


LEGENDRE FUNCTIONS
------------------

ALL WRAPPED



SPHERICAL HARMONIC TRANSFORMATIONS
----------------------------------

-- wrapped: --
MakeGridDH
SHExpandDH
MakeGridDHC
SHExpandDHC
MakeGridGLQ
SHExpandGLQ
MakeGridGLQC
SHExpandGLQC
PreCompute (partially - optional parameter PLX not implemented)
SHRead     (partially - optional parameters)
DHaj
GLQGridCoord
PreGLQ
NGLQ
NGLQSH
NGLQSHN
MakeGrid2D (if f,a < 0, they are treated as not present, default is f=-1,a=-1)

-- unwrapped: --

SHExpandLSQ
MakeGridPoint
SHMultiply



GLOBAL SPECTRAL ANALYSIS
------------------------

ALL WRAPPED

-- issues: --
SHAdmitCorr (always returns ADMIT_ERROR)



IO/STORAGE/CONVERSIONS
----------------------

-- wrapped: --
SHRead (partially - some optional arguments not implemented)
SHCilmToCindex
SHCindexToCilm
SHCilmToVector
SHVectorToCilm
YilmIndex
SHrtoc
SHctor



SPHERICAL HARMONICS ROTATIONS
-----------------------------

ALL WRAPPED



LOCALIZED SPECTRAL ANALYSIS
---------------------------

-- not wrapped: --
SHBiasAdmitCorr
SHMTDebias
SHMTVarOpt0
SHSjkPG0
SHSjkPG

-- wrapped: --
SHBiasK
SHBias
ComputeD0
ComputeDm
ComputeDG82
SHFindLWin
SHLocalizedAdmitCorr (k1linsig and taper_wt are not used if negative (default))
SHMultiTaperCSE (if first weight is negative (default) they are not used)
SHMultiTaperSE (if first weight is negative (default) they are not used)
SHReturnTapers
SHReturnTapersM
SHReturnTapersMap
ComputeDMap
SphericalCapCoef
SHBias
Curve2Mask (has a bug?! vertical lines can appear for specific values)
